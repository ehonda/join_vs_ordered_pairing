using JoinVsOrderedPairing.Extensions;
using System;
using System.Collections.Generic;
using System.Linq;

namespace JoinVsOrderedPairing
{
    class Program
    {
        static void Main(string[] args)
        {
            //var left = new List<int> { 1, 2, 3, 4, 5 }.OrderBy(i => i);
            //var right = new List<int> { 1, 4, 6 }.OrderBy(i => i);

            //var paired = left.PairSelect(right, i => i, i => i).ToList();
        }
    }
}
